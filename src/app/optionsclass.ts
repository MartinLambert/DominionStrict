import { optionValues } from './optionValues'

export class Options {
	attacks = optionValues.RANDOM
	trash   = optionValues.RANDOM
	village = optionValues.RANDOM
	draw    = optionValues.RANDOM
	buy     = optionValues.RANDOM
	potions = optionValues.MIN3
	costs   = optionValues.BALANCED
	sortBySet = 'no'
}
